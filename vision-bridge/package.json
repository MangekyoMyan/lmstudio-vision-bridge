{
  "name": "lmstudio-vision-bridge-generator",
  "version": "0.1.0",
  "private": true,
  "description": "LM Studio Generator plugin: bridges MCP tool-result images (saved in the working directory) into the loaded model's vision input via the local OpenAI-compatible API. Tool execution stays with LM Studio.",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "dev": "lms dev"
  },
  "dependencies": {
    "@lmstudio/sdk": "1.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
